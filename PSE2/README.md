- Uses Gemini (`models/gemini-1.5-flash`) for general answers.
- Routes math questions to `calculator_tool.py`.
- Fails if multi-step reasoning is attempted.

Setup
-pip install -r requirements.txt

